import { assert } from "chai";
import { assertSubset } from "../test-utils.js";
import { placetimeService } from "./placetime-service.js";
import { john, summerTrip, testPlacegroups, testPlaces, beachKerry } from "../fixtures.js";

suite("Place API tests", () => {
  let user = null;
  let strangePlaces = null;

  setup(async () => {
  });

  teardown(async () => { });

  test("create place", async () => {
  });

  test("create Multiple placeApi", async () => {
  });

  test("Delete Place", async () => {
  });

  test("test denormalised placegroup", async () => {
  });
});
